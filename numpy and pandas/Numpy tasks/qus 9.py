import numpy as np
new_array = np.empty(4)
print(new_array)
'''
The function does not initialize the array's elements. It alocates a block of memory of the size given and type. The random values are just garbage values and do not have any effect on future operations as they are very insignificant values.
'''
